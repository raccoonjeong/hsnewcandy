<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.candy.mapper.FreeboardMapper">

	
	<insert id="register">
	insert into tbl_freeboard (title, content, writer) 
	values (#{title},#{content},#{writer}); 
	</insert>

	<update id="modify">
	update tbl_freeboard set title=#{title}, content=#{content}, updatedate=now() 
	where bno=#{bno};
	</update>
	
	<select id="read" resultType="org.candy.domain.FreeboardVO">
		select * from tbl_freeboard where bno = #{bno};
	</select>

</mapper>